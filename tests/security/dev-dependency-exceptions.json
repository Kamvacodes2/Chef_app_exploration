{
  "$comment": [
    "Named, time-limited exceptions for DEVELOPMENT-ONLY dependency advisories.",
    "",
    "This file is NOT a suppression list for the production audit gate. The",
    "production gate lives in tests/security/dependencies.test.ts, runs",
    "`pnpm audit --json --prod`, fails on any high or critical finding, and does",
    "not read this file. The two mechanisms are deliberately separate.",
    "",
    "Scope rules enforced by tests/security/devDependencyExceptions.test.ts:",
    "  1. every entry must carry all required fields;",
    "  2. every entry must have a future review/expiry date - an expired entry",
    "     fails the suite, which forces a decision instead of silent drift;",
    "  3. an advisory is only excused if `pnpm audit` reports every one of its",
    "     findings as dev-only. A production finding is never excused here;",
    "  4. any dev-only advisory that is NOT listed here fails the suite, so a",
    "     newly introduced advisory cannot arrive unnoticed."
  ],
  "policyVersion": 1,
  "exceptions": [
    {
      "advisoryId": "GHSA-82fw-gwwq-j7x9",
      "module": "vitest",
      "severity": "moderate",
      "title": "Vitest: Path Traversal / Arbitrary File Read via @vitest/mocker Redirect Mock",
      "vulnerableVersions": ">=2.1.0 <4.1.11",
      "patchedVersions": ">=4.1.11",
      "justification": "Development and test runner mock utility path traversal advisory affecting unit testing toolchain; isolated to local CI and test environments with no production exposure.",
      "scope": "dev-only test runner toolchain",
      "owner": "platform-team",
      "createdOn": "2026-09-14",
      "reviewBy": "2026-12-31",
      "removalCondition": "Upgrade to resolved vitest/coverage toolchain when next minor release stabilizes in the workspace."
    }
  ]
}
