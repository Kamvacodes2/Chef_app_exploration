/** How long (ms) each frame stays on screen before auto-advancing. */
export const INTRO_ADVANCE_INTERVAL_MS = 4000;

/** Crossfade duration (ms) between frames. */
export const INTRO_CROSSFADE_MS = 700;
