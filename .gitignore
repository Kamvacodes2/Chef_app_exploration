node_modules
.next
out
dist
coverage
apps/web/.coverage.*
playwright-report
test-results
.env*
!.env.example
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo

# Scratch directory written and removed by the database suite's rollback test.
tests/db/.scratch-migrations
.pnpm-store/
.local-dev/

# Quarantined dependency trees left by containerized deploy runs. The release
# harness dot-renames them in place; tsc/vitest skip hidden dirs and Prettier
# ignores them via .prettierignore.
node_modules.stale-*
.node_modules.stale-*
