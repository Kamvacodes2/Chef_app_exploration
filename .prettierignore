node_modules
dist
.next
coverage
playwright-report
test-results
pnpm-lock.yaml
apps/web/public
apps/web/Assets
apps/web/next-env.d.ts
*.tsbuildinfo
.agents
.claude
.codex

# Prose, not code. `plans/` is the execution blueprint and the progress ledger;
# `docs/` holds the ADRs and contract characterization. Re-wrapping human-authored
# markdown would churn the record without improving anything, and the progress
# ledger is owned outside this step.
plans
docs
